# Copy to .env.local. Only needed for the AI interpretation layer.
ANTHROPIC_API_KEY=
# Optional overrides
# RESIDUAL_LLM_MODEL=claude-sonnet-5
# SEC_USER_AGENT="Your Name your@email.com"
# Optional network diagnostic for permitted networks only; normal OS DNS is the default.
# RESIDUAL_ENABLE_DOH=1

# Private Bitget Demo verification is intentionally not used by replay/live paper mode.
# Add these only when performing the separate read-only Demo account check.
# BITGET_API_KEY=
# BITGET_SECRET_KEY=
# BITGET_PASSPHRASE=
